#include <iostream>
#include <math.h>
using namespace std;
/*//option a
int main()
{
	int n;
	float a, b;
	double h;
	float r[100];
	cout << "Enter n:";
	cin >> n;
	cout << "Enter a:";
	cin >> a;
	cout << "Enter b:";
	cin >> b;
	if (a == b)
	{
		cout << "error";
	}
	h = (b - a) / n;
	for (int i = 0; i < n; i++)
	{
		r[i] = a + i * h;
		cout << r[i] << endl;

	}
	return 0;
}

//option b
int main()
{
	float a[1001];

	cout << "Elements less than 0.25:" << endl;
	for (int k = 1; k <= 512; k *= 2)
	{
		a[k] = pow(sin(3 * k + 5), 2) - pow(cos(k * k - 15), 2);
		if (a[k] < 0.25)
		{
			cout << "a"<< k << " = ";
			cout<< a[k] << endl;
		}
	}
	return 0;
	*/

