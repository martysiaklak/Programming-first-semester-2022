#include <iostream>
using namespace std;
/*enum dogbreed
{
	HASKI,
	BULDOG,
	TAKSA,
	CHIHUAHUA

};
void add_el(dogbreed * array, int size); //чому через вказівник?
int main()
{
	cout << "Enter size:" << endl;
	int size;
	cin >> size;
	dogbreed* array_breed = new dogbreed[size];
	add_el(array_breed, size);
	return 0;
}
void add_el(dogbreed* array, int size)
{
	for (int i = 0; i < size; i++)
	{
		array[i] = dogbreed(rand() % 4);
		switch(array[i])
			{
		case 0:
		{
			cout << "breed-->" << "HASKI" << endl;            
		}
		break;
		case 1:
		{
			cout << "breed-->" << "BULDOG" << endl;
		}
		break;
		case 2:
		{
			cout << "breed-->" << "TAKSA" << endl;
		}
		break;
		case 3:
		{
			cout << "breed-->" << "CHIHUAHUA" << endl;
		}
		break;

			}
	}
}*/
